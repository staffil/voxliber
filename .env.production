# ========================================
# 프로덕션 환경 설정 (AWS 서버용)
# ========================================
# 이 파일을 AWS 서버에 .env로 복사하여 사용하세요
# ⚠️  주의: 이 파일을 Git에 커밋하지 마세요!

# Django 설정
DEBUG=False

# 시크릿 키 생성 방법:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=여기에-새로운-시크릿-키-생성-필요

# AWS EC2 퍼블릭 IP (EC2 대시보드에서 확인)
# 예: AWS_EC2_IP=13.209.123.456
AWS_EC2_IP=

# 데이터베이스 설정 (MySQL)
DB_NAME=voxliber
DB_USER=chung
DB_PASSWORD=여기에-실제-DB-비밀번호-입력
DB_HOST=localhost
DB_PORT=3306

# 외부 TTS 서비스 URL (프로덕션용 엔드포인트)
# Ngrok 대신 실제 프로덕션 서버 URL 사용
EXTERNAL_TTS_URL=https://your-production-tts-server.com
